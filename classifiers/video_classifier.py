# classifiers/video_classifier.py

class VideoClassifier:
    def __init__(self):
        # Placeholder: later we’ll use face + expression detection
        pass

    def predict(self, video_path):
        # For now, just return a dummy result
        print(f"[VideoClassifier] Received input: {video_path}")
        return "neutral", 0.5
     