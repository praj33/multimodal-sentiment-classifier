## 🔌 Python SDK (Developer Usage)

You can use the `sentiment_sdk` to access the API from any Python script.

### Install requirements

Make sure the FastAPI server is running:

```bash
python -m uvicorn api:app --reload
